// Playground for previewing components during development. Not part of
// the published package. Empty for now — build components in src/components
// (or vendor shadcn primitives into src/components/ui via `npm run ui:add`),
// export them from src/index.ts, and preview them here.
export function App() {
  return (
    <div className="flex min-h-svh items-center justify-center text-sm text-muted-foreground">
      Nothing to preview yet.
    </div>
  );
}
